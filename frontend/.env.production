# Loaded automatically by `vite build` (not secret — public API origin).
# Override in Vercel → Environment Variables as VITE_API_URL if your Railway URL changes.
VITE_API_URL=https://sentinel-scanner-production-6f15.up.railway.app
