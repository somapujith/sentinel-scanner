# Browser calls use same-origin /api (Vite proxy locally; Vercel rewrites to Railway — see ../vercel.json).
# Set VITE_API_URL only if you host the UI somewhere without /api rewrites (e.g. some static hosts).
